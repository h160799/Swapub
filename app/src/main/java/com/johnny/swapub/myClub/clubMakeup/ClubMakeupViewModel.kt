package com.johnny.swapub.myClub.clubMakeup

import androidx.lifecycle.ViewModel
import com.johnny.swapub.data.remote.SwapubRepository

class ClubMakeupViewModel(
    val swapubRepository: SwapubRepository
) : ViewModel() {
}